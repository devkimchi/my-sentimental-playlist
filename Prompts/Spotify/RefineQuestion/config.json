{
  "schema": 1,
  "type": "completion",
  "description": "Refine the user's request based on the identified measurement",
  "execution_settings": {
    "default": {
      "max_tokens": 800,
      "temperature": 0
    }
  },
  "input_variables": [
    {
      "name": "input",
      "description": "The user's request",
      "required": true
    },
    {
      "name": "sentiment",
      "description": "The user's measured sentiment in JSON format",
      "required": true
    }
  ]
}
